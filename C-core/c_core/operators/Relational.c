// Relational ope.
#include <stdio.h>

int main() {
    // 0 - False
    // 1 - True
    int a = 10, b = 20, res;
    // res = a == b;
    // res = a != b;
    // res = a < b;
    // res = a > b;
    // res = a <= b;
    res = a >= b;
    printf("%d", res);

    return 0;
}