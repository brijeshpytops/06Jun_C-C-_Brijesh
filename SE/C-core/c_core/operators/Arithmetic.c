// Arithmetic ope.
#include <stdio.h>

int main() {
    int a = 10, b = 20, res;
    // res = a + b;
    // res = a - b;
    // res = a * b;
    // res = a / b;
    res = a % b;
    printf("%d", res);

    return 0;
}